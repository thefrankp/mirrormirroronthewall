{
  "name": "mirrormirroronthewall",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"nodemon server/index.js\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "sync": "npm run sync --prefix client",
    "deploy:ios": "npm run deploy:ios --prefix client",
    "deploy:android": "npm run deploy:android --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2",
    "nodemon": "^3.1.4"
  }
}
